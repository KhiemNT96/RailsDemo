= form_with(model: productcategory, local: true) do |form|
  - if productcategory.errors.any?
    #error_explanation
      %h2
        = pluralize(productcategory.errors.count, "error")
        prohibited this productcategory from being saved:
      %ul
        - productcategory.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :CategoryID
    = form.text_field :CategoryID, id: :productcategory_CategoryID
  .field
    = form.label :CategoryName
    = form.text_field :CategoryName, id: :productcategory_CategoryName
  .actions
    = form.submit
